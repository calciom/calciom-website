# wrangler.toml
# Native static site deployment configuration for Calciom

name = "calciom-site"
compatibility_date = "2026-09-22"

# Inline configuration directing Cloudflare to serve the public folder
assets = { directory = "./public" }
run_worker_first = false
